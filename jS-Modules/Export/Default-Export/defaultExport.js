// Default Export
// You can only have one default export in a file

const message = () => {
    const name = 'CR7';
    const age = 39;
    return name + ' is ' + age + ' years old.';
};

export default message;