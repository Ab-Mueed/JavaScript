// The size Property (! Not a Method)
// The size property returns the number of elements in a Map.

const fruits = new Map([
    ['Apple', 200],
    ['Grapes', 300],
    ['Banana', 500]
]);
console.log(fruits.size); // 3
